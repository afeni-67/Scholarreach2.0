name: Scholarreach Hunt (MOVED)

on:
  workflow_dispatch:

# Auto-hunt moved off this account. Do not enable.
# Custom-journal extract uses processor.yml only.

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Moved
        run: |
          echo "Auto-hunt was removed from this repo."
          echo "Use the dedicated hunt GitHub account/repo."
          exit 0
