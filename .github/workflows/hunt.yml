name: Scholarreach Hunt (DEPRECATED — use processor)

on:
  workflow_dispatch:

# DEPRECATED: Hunt is now 2 labeled jobs inside processor.yml (workers 19–20).
# Do not dispatch this workflow from Render. Kept only so old links do not 404.

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated
        run: |
          echo "This workflow is deprecated."
          echo "Hunt (topic+email) runs as matrix.role=hunt inside processor.yml."
          echo "Cancel any queued runs of this workflow."
          exit 0
